{
  "name": "api-lambda-save-dynamodb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "package": "aws cloudformation package --template-file template.yaml --output-template-file output.yaml --s3-bucket api-lambda-save-dynamodb-bucket",
    "deploy": "aws cloudformation deploy --template-file output.yaml --stack-name api-lambda-save-dynamodb --capabilities CAPABILITY_IAM",
    "qd": "npm run package && npm run deploy"
  },
  "keywords": [],
  "author": "Aleksandar Simovic",
  "license": "MIT"
}
